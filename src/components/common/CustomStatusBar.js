import React from "react";
import { StatusBar } from "react-native";

const CustomStatusBar = props => {
  return <StatusBar backgroundColor="#090A21" barStyle="light-content" />;
};

export default CustomStatusBar;
